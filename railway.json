{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "mkdir -p /app && cp -r website/backend/* /app/ && cp *.py /app/ && cd /app && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd /app && python -m uvicorn app:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
